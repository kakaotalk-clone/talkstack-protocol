{
  "name": "@kakaoclone/protocol",
  "version": "1.0.0",
  "description": "카카오톡 클론 WebSocket 프레임 타입 (schema/*.proto 에서 생성)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "type": "module",
  "scripts": {
    "generate": "buf generate .. --template buf.gen.yaml",
    "build": "npm run generate && tsc",
    "clean": "rimraf dist src/generated",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.0"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.47.2",
    "rimraf": "^6.0.1",
    "ts-proto": "^2.2.5",
    "typescript": "^5.6.3"
  }
}
